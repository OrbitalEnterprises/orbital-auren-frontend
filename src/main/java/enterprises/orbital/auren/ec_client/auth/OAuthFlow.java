package enterprises.orbital.auren.ec_client.auth;

public enum OAuthFlow {
                       accessCode,
                       implicit,
                       password,
                       application
}